[project]
name = "docs-server"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["mcp>=1.0", "rank-bm25>=0.2"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/docs_server"]
