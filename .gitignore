CLAUDE.local.md
.claude/settings.local.json
.env

# 로컬 TLS 인터셉터 CA (머신 종속 — certs/README.md 참고)
certs/*.crt

# Python
.venv/
__pycache__/
*.db
.pytest_cache/
.ruff_cache/

# audit log (append-only JSONL — 런타임 산출물)
audit/
